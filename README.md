# Mini FPS Shooting Game

[![Play now](https://img.shields.io/badge/%E2%96%B6%20Play%20now-minifpsshootinggame.netlify.app-00C7B7?logo=netlify&logoColor=white)](https://minifpsshootinggame.netlify.app)
[![Deploy WebGL to Netlify](https://github.com/Ishaan-Kaneria/Mini_FPS_Shooting_Game/actions/workflows/deploy-web.yml/badge.svg)](https://github.com/Ishaan-Kaneria/Mini_FPS_Shooting_Game/actions/workflows/deploy-web.yml)
[![Unity 6000.6.0f1](https://img.shields.io/badge/Unity-6000.6.0f1-000000?logo=unity&logoColor=white)](https://unity.com/releases/editor/archive)
[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)

A level-based first-person shooter built in **Unity 6000.6.0f1** on the Universal Render Pipeline. Six arenas, eight levels each: a fixed crowd of NavMesh-driven enemies, a strict clock, and one to three stars depending on how much of it you cleared before the clock beat you. Clear a level to unlock the next one, and spend what the kills paid on a store of guns, bombs, energy drinks and upgrades.

The whole game is generated by an editor tool. There is no scene to hand-assemble: pick a theme from the **FPSKit** menu, press Play.

---

## Play it in your browser

**[▶ Play now](https://minifpsshootinggame.netlify.app)** — nothing to install. No Unity, no account, no download.

<!-- Drop a screenshot or a short GIF in docs/ and uncomment this line:
![Gameplay](docs/gameplay.gif)
-->

| | |
|---|---|
| Move | **W/A/S/D** (arrow keys work too) |
| Look | Mouse — click the game once to capture the cursor |
| **Shoot** | **Left click** |
| Aim down sights | Right click |
| Jump | Space (you get a double jump) |
| Sprint | Left Shift |
| Crouch | Left Ctrl, or C |
| Reload | R |
| Pause, or release the mouse | Escape |

On a phone or tablet the on-screen controls appear instead: left thumb to move, drag the right half to look, buttons for fire, aim, jump, reload, bomb and drink.

Kills pay coins, headshots pay more, and stars pay most. Spend them in the store between levels.

Pick an arena, pick a level, kill everything before the clock runs out. Clearing a level outright is three stars; falling short is one or two; falling too far short means the level stays locked and you go again. On a boss level the boss is most of the score, so leaving it standing costs a star however much of its escort you cleared.

> These are the `StandardFPS` preset, which is what `FPSKit_Generated/Controls.asset` ships. Every binding is editable on that asset, and its inspector has one-click presets for two arrow-key schemes — see [Controls](#controls) below.

---

## Requirements

| | |
|---|---|
| Unity | **6000.6.0f1** (Unity 6) |
| Render pipeline | Universal RP 17.6.0 |
| Packages | `com.unity.ai.navigation`, `com.unity.inputsystem`, TextMeshPro (via uGUI) |
| Platforms | Windows / macOS / Linux desktop, and WebGL for the browser build. On-screen touch controls are optional and added by their own menu item |

---

## Quick start

1. Clone the repo and open it in Unity 6000.6.0f1.
2. From the menu bar: **FPSKit → Build Scene → Industrial Warehouse**.
3. Press **Play**.

That one menu item creates the tags and layers, an arena, the player rig with a working gun, the enemy prefab and archetypes, spawn points, a baked NavMesh, the level manager and that arena's ladder of levels, post-processing and the full HUD — then saves the scene to `Assets/FPSKit_Generated/Scenes/`.

Play in the editor always starts at the dashboard, whatever scene is open. Run **FPSKit → Build Dashboard** once so the arena cards and their level ladders exist.

Six themes ship in the box:

| Theme | Character |
|---|---|
| Industrial Warehouse | Overcast steel and concrete. The neutral baseline. |
| Desert Outpost | High sun, bleached sand, long sightlines. |
| Snowbound Station | Low winter sun, thick fog, short engagements. |
| Night Rooftop | City night, warm practicals, heavy bloom. |
| Abandoned Subway | Tight, dark, claustrophobic. |
| Mars Colony | Rust and dust under a thin sky. |

**FPSKit → Build Scene → Build ALL Themes** builds all six and registers them in Build Settings so the in-game restart works.

---

## Controls

`Assets/FPSKit_Generated/Controls.asset` ships the **Standard FPS** preset:

| Action | Default |
|---|---|
| Move | **W/A/S/D** (arrow keys also work) |
| Fire | **Left click** |
| Aim down sights | Right click |
| Jump | **Space** (double jump available) |
| Sprint | **Left Shift** |
| Crouch | Left Ctrl or C |
| Reload | R |
| **Throw a bomb** | Hold **G** to aim, release to throw. Right click while aiming locks the range |
| Drink an energy drink | **F** |
| Pause | Escape |
| Replay the level (on the results screen) | R |
| Next level (on the results screen) | Space |
| Back to the dashboard | Q |

Nothing is hard-coded. Every binding lives on that `ControlSettings` asset, and its inspector has one-click presets for the two arrow-key schemes as well:

| Preset | Move | Fire | Jump | Sprint |
|---|---|---|---|---|
| **Standard FPS** (shipped) | Arrows **and** W/A/S/D | Left click | Space | Left Shift |
| **Arrows and Mouse** | Arrows only | Left click | Space | Left Shift |
| **Arrows and Space** | Arrows only | **Space** | **Left click** | Double-tap Space |

Applying a preset overwrites every binding, so retune afterwards rather than before. Read new keys through the `ControlSettings` helpers rather than naming a `KeyCode` literal, or rebinding stops working for that action.

Touch controls — virtual joystick, look area and buttons — are added by **FPSKit → Add Mobile Touch Controls**, and the browser build bakes them in automatically. They show themselves only on a device whose *only* pointer is a finger, so a touchscreen laptop keeps mouse look.

---

## How the game plays

### Levels

An arena is a ladder of eight levels, and a level is one fight with a fixed shape: a set number of enemies, a strict clock, and a score cut into stars at the end.

- **Three stars** — every enemy dead, boss included, before the clock ran out.
- **Two stars** — 80% of the level's weight killed.
- **One star** — 50%. This is also the pass mark, and passing is what unlocks the next level.
- **No stars** — try again. The level stays locked.

The score is *weighted*, not counted. Each ordinary enemy is worth one and the boss is worth what its level says it is worth — five or more — so on a boss level the boss is a fifth of the total on its own. Clearing the whole escort and leaving the boss alive is one star, which is the point: the boss is the level.

Stars are kept per arena and per level in `PlayerPrefs`, and the level select shows them, so a level you three-starred stays three-starred however many times you replay it. Replaying can only ever improve a result.

Difficulty moves across the ladder in four numbers at once: more enemies, less clock per enemy, tougher bodies and a nastier mix. The mix is the interesting one — each level names a difficulty step, and the roster gates its variants by step, so the enemies you meet on level 7 are not the level-1 enemies with bigger numbers. The six arenas are all open from the start and each one is pitched a little harder than the last, so the later arenas are somewhere to go once the earlier ones stop being a fight.

The rifle keeps up. Entering level *n* hands you the magazine, damage and reload of a rifle that has been through *n*−1 levels, so a later level is a harder fight rather than a longer one. Nothing is ever written back to the weapon asset.

The attack wind-up is the one number the difficulty curve is *not* allowed to grind away — it compresses only part way and never past a floor, because that telegraph is the player's reaction window. Difficulty without it is just unfairness.

### The clock is strict, and nothing may stall it

A level ends on the last kill or on the clock, whichever comes first, and the clock is checked without exception. That matters more than it sounds: the kit is meant to run in imported levels, and in a real level an enemy that falls through a gap in the geometry never dies, never arrives and never stops being counted. A level that waited for it would be a softlock.

Two systems stop that happening:

- **The enemy leash.** Anything that gets more than ~95 m away, falls more than 60 m below you, or whose agent leaves the NavMesh is removed after a few seconds' grace. Discarding is not killing — it scores nothing and drops nothing — and a fresh enemy is put in the queue in its place, so a hole in the floor cannot quietly make three stars impossible.
- **The clock itself.** The backstop for whatever the leash misses: something wedged on geometry, stuck behind a door, or standing on an island of NavMesh it cannot leave. The level is scored on what was actually killed.

Enemies arrive from every direction. Spawn bearings advance by the golden angle rather than being drawn independently, because independent random draws clump — roll a dozen and several land within a few degrees, which is what funnels a whole level through one corner of the map.

### Boss levels

Every third level, and always the last one in an arena, sends a boss alongside its escort with its own health bar across the top of the screen. Bosses cannot be staggered, and later ones carry more health on top of the level curve. Which boss a level uses is named in the level asset, so it walks up the roster as the ladder climbs rather than sending the same one three times.

### Coins and the store

Every kill pays coins, a headshot pays extra, and finishing a level pays a bonus per star
and a bonus scaled by score. They are banked once, when the level is scored — walking out
halfway still pays for what you killed, but not for the stars you did not earn.

The **Store** button on the dashboard opens four shelves:

| Shelf | What is on it |
|---|---|
| **Weapons** | Six guns: the free service rifle, an SMG, a shotgun, a burst rifle, a DMR and an LMG. Buy one, equip it, take it into the next level. |
| **Explosives** | Three bombs: the free frag, a wide cluster charge and a thermite shell that puts everything it has into four metres. |
| **Supplies** | Energy drinks and adrenaline shots. Bought in packs, carried into a level, spent with **F**. |
| **Upgrades** | Permanent maximum health and shield. |

Each gun and bomb takes **five upgrades** and no more. A gun upgrade buys damage, bullets
per second, magazine size and reload speed; a bomb upgrade buys damage, blast radius and
eventually another charge. Health is the one upgrade with **no cap** — it is what a player
stuck on a level can always put coins into.

Every upgrade price is `base × growth^level`, so the fifth costs around six times the
first. That is the point: without escalation the tenth upgrade costs what the first did,
and by then coins are not scarce.

Nothing bought is written into the weapon or bomb asset. Those are shared
ScriptableObjects — a bonus stored in one would survive quitting the game and compound on
every restart — so upgrades are multipliers on the components, and the assets stay the
baseline.

### Bombs

Hold **G** and a ring appears on the ground where the blast will land, with a dotted arc
showing how the bomb gets there. The ring is drawn at the bomb's real damage radius, so
two enemies standing inside it are two enemies the bomb is going to hit. Release to throw;
it lands in **one second**, every time.

Right click while aiming to **lock the range**. Unlocked, the ring slides along the floor
wherever you look, which is what you want for placing one precisely. Locked, the distance
is pinned and turning sweeps the ring around you at that radius, which is what you want
when the fight is moving and the distance is the part you already got right.

Damage falls off with distance from the centre on a curve, so landing it well is worth
real damage and clipping the edge is not. It hurts you too, at a fraction — a bomb you can
drop on your own feet for free is a bomb with no aiming in it.

Charges come back every level rather than being spent forever. A bomb you are afraid to
spend is a bomb that never gets thrown.

### Energy drinks

Health and shield back on the spot, then ten seconds or so of moving, shooting and
reloading faster. The rush does not stack — a second drink resets the clock instead of
doubling the speed, because a player at three times walking pace is a player whose level
has stopped being a fight.

### Enemies

One base prefab, many variants. Each variant is a data asset, not a prefab or a subclass:

| Enemy | From step | What it does |
|---|---|---|
| **Grunt** | 1 | Basic melee. Numerous early, thins out later. |
| **Runner** | 2 | Fast, fragile, charges the last stretch. Punishes camping. |
| **Marksman** | 3 | Holds a firing line and shoots. Makes cover matter. |
| **Brute** | 4 | Huge, armoured, hits like a truck. Telegraphs hard. |
| **Sentinel** | 7 | Shielded burst-fire elite. Break the armour first. |
| **Screamer** | 9 | Small, quick, always circling. Flanks relentlessly. |
| **Warden** | 5 | *Boss.* An enormous armoured melee threat with a long reach. |
| **Harbinger** | 10 | *Boss.* Hangs back and shells the arena in bursts. |

Under the hood every one of them runs the same brain, layered rather than scripted per type: line-of-sight checks, crowd separation so they surround you instead of queuing up, strafing and flanking approaches, a charge burst in mid-range, a colour-flash telegraph before each attack, and a stagger reaction when a big hit lands.

### Health

Both sides use the same two-layer system — a **shield** that soaks damage first and regenerates quickly, over **health** underneath that comes back slowly or not at all.

- The player has 100 health and a 50-point shield, plus a quarter-second of immunity after each hit so a crowd of melee enemies cannot chain-delete you with no window to react.
- Enemies show a floating bar only once they are actually hurt. Elites and bosses carry a visible armour segment you have to break before the kill starts counting.
- Enemies drop health and shield cells; walk near one to collect it. A pickup refuses to be consumed when it would be wasted, so it stays on the floor for later.

### Score

Kills score, headshots score more, and chaining kills inside a short window builds a multiplier up to 4×. Taking a hit breaks the chain — the scoreboard rewards staying in the fight, not retreating to a corner. Clearing a level pays a bonus scaled by the level number, a bonus per second left on the clock, and a bonus per star.

Stars, per-level best scores, total kills and the overall best score are kept in `PlayerPrefs` between sessions. The dashboard shows the stars taken against the stars available across every arena.

---

## Extending it

Both extension points are ScriptableObjects. Adding content never means editing the builder.

### A new arena = a new `LevelTheme` asset

1. Duplicate any asset in `Assets/FPSKit_Generated/Themes/`.
2. Retune it in the inspector — sky, sun, fog, palette, arena size, how many rooms / platforms / cover walls / crates / pillars, prop prefabs, weather, ambience, post-processing.
3. Select it in the Project window and run **FPSKit → Build Scene → From Selected Theme Asset**.

The six menu entries are just shortcuts to the built-in assets; the tool builds any theme you hand it.

### A new enemy = a new `EnemyArchetype` asset

1. Duplicate any asset in `Assets/FPSKit_Generated/Enemies/`.
2. Change the numbers — health, damage and speed multipliers, size, colour and glow, melee or ranged, attack range, wind-up, burst size, how hard it strafes, whether it charges, how much damage staggers it, score value, drop chances.
3. Set its unlock step and weight curve, then add it to the LevelManager's roster in the scene.

The archetype is stamped onto an instance of the shared `Enemy.prefab` at spawn. Set `Role` to **Boss** and it is drawn only for boss levels; set it to **Elite** and it stays part of the normal mix.

### A new level = an entry in a `LevelSet` asset

The levels an arena offers live in `Assets/FPSKit_Generated/Levels/Levels_<Arena>.asset`, one list of entries. Add one in the inspector — enemy count, clock, how many can be alive at once, whether it has a boss and which, difficulty multipliers, difficulty step, star thresholds — and re-run **FPSKit → Build Dashboard** so the arena card picks up the new count. The level select clones a tile per entry at runtime, so the menu scene never needs rebuilding for new levels.

`FPSKit → Reset Level Sets to Defaults` re-stamps the built-in curve over every set. Like the enemy archetypes, the assets are generated once and then left alone, so a number changed in `FPSKitLevels.Configure` does **not** reach the assets the game reads until that is run. Stars the player has earned are stored separately and are never touched by it.

### A new weapon = a new `WeaponData` asset

**Assets → Create → FPSKit → Weapon Data** gives you fire mode (single/burst/auto), rounds per minute, damage and falloff, pellet count for shotguns, magazine and reserve, spread behaviour, recoil, ADS position and FOV, and audio/VFX hooks.

To put it in the shop, add an entry to `Assets/FPSKit_Generated/Store/Store.asset` with a
**new, permanent `id`** — ownership and upgrade level are filed under it, so renaming one
forgets every purchase of that item. The store clones a card for it at runtime.

### A new bomb = a new `BombData` asset

**Assets → Create → FPSKit → Bomb Data**: centre damage, blast radius, how sharply the
damage falls off, what the edge is worth, self-damage fraction, fall time, throw range,
charges per level, and how high it arcs. Add it to the catalog the same way a gun is
added; the prefabs and audio are stamped on by the next scene build.

`FPSKit → Reset Store to Defaults` re-stamps the built-in stock and prices. Like the
enemy archetypes and the level sets, the assets are generated once and then left alone,
so a price changed in `FPSKitStore.Configure` does **not** reach the asset the game reads
until that is run. Coins already earned and items already bought live in `PlayerPrefs`
and are never touched by it.

### Dropping gameplay into a level you already built

**FPSKit → Add Gameplay To Current Scene** is the non-destructive path. It injects the player, enemies, level manager, HUD, post-FX and a baked NavMesh into whatever scene is open, leaving your geometry and baked lighting alone. Colliders get moved to the Environment layer and meshes without colliders get one, so AI sight lines and bullet hits work.

This is the path for an imported environment, and the LevelManager's defaults are tuned for one. The knobs worth checking against your map:

| Field | Default | What to watch for |
|---|---|---|
| `Min Spawn Distance From Player` | 18 m | Your breathing room. Raise it if enemies arrive on top of you. |
| `Max Spawn Distance From Player` | 30 m+ | Must sit inside your baked NavMesh or spawns fail. |
| `Despawn Distance` | 95 m | Kept above the spawn ring automatically — a leash shorter than the ring would delete enemies on arrival. |
| `Fall Kill Depth` | 60 m | Lower it if your level has legitimate drops deeper than this. |
| `Replace Lost Enemies` | on | Re-queues whatever the leash discards, so geometry cannot cost the player a star. |
| `Levels` | the arena's set | Leave it empty and you get one stand-in level, not the ladder. |

> ⚠️ **`Build Scene` is destructive.** It replaces the open scene entirely and overwrites the saved theme scene. Fix generated scene content by editing the builder, not the `.unity` file — the next build discards hand edits.

---

## Project layout

```
Assets/
  Scripts/
    Runtime/                  gameplay, flat, global namespace
    Editor/                   FPSKit.EditorTools, all #if UNITY_EDITOR
  FPSKit_Generated/           tool output - regenerable
    Scenes/  Themes/  Levels/  Enemies/  Store/  Materials/
  WebGLTemplates/FPSKit/      the page the browser build ships
  Plugins/WebGL/              jslib the page and the game share
  RPG_FPS_game_assets_industrial/   art pack
Tools/
  unity-batch.sh              headless editor driver
  deploy-web.sh               rebuild and publish in one command
```

`Assets/Scripts/Editor/` must stay named `Editor` — that folder name is what keeps editor code out of player builds. There are no `.asmdef` files.

---

## Headless builds

The scene builder can run without opening the editor, which is what makes it usable from CI:

```bash
Tools/unity-batch.sh                                    # compile check only
Tools/unity-batch.sh FPSKitBatch.BuildAllThemes         # rebuild every scene
Tools/unity-batch.sh FPSKitBatch.BuildTheme -fpskitTheme "Mars Colony"
Tools/unity-batch.sh FPSKitBatch.VerifyBuild            # build one scene, assert it is playable
Tools/unity-batch.sh FPSKitBatch.VerifyLevels           # a level is scored on both its endings
Tools/unity-batch.sh FPSKitBatch.VerifyStore            # coins in, a gun and an upgrade out
Tools/unity-batch.sh FPSKitBatch.VerifyFlow             # dashboard -> level select -> arena -> back
Tools/unity-batch.sh FPSKitBatch.ResetLevelSets         # re-stamp the built-in level curve
Tools/unity-batch.sh FPSKitBatch.ResetStore             # re-stamp the built-in stock and prices
Tools/unity-batch.sh FPSKitBatch.VerifyReplay           # play three times over, assert every run is clean
Tools/unity-batch.sh FPSKitBatch.VerifyStatics          # every static resets between play sessions
```

### Publishing a browser build

```bash
Tools/unity-batch.sh FPSKitBatch.BuildWebGL -buildTarget WebGL -fpskitFallback false
```

The page that build ships is `Assets/WebGLTemplates/FPSKit/index.html`, not Unity's stock template: full-viewport canvas, the control legend on screen during the download, an error panel instead of `alert()`, and the pointer/keyboard handling a canvas game needs in a browser. `Build/WebGL/index.html` is output — change the template, not the copy in the build.

Writes `Build/WebGL/`, which is gitignored — a WebGL payload is tens of megabytes and git would keep it forever. Upload the folder to any static host; drag it onto [app.netlify.com/drop](https://app.netlify.com/drop) and you have a link in about a minute.

To rebuild and republish in one step:

```bash
Tools/deploy-web.sh              # rebuild, then deploy
Tools/deploy-web.sh --no-build   # deploy what is already built
Tools/deploy-web.sh --dry-run    # build and check, stop before uploading
```

It needs `NETLIFY_AUTH_TOKEN` and `NETLIFY_SITE_ID` in the environment and talks to Netlify's deploy API with `curl`, so there is no Node toolchain to install for one command. It refuses to upload a folder the build did not just write — publishing a stale build is the one failure that looks exactly like success from the outside.

There is no content-only update: Unity bakes every asset into `WebGL.data.br`, so changing a single number means a full rebuild. Rebuilds after the first are faster, because shader compilation dominates and that cache survives.

### Continuous deployment

`.github/workflows/deploy-web.yml` does the same thing on every push to `main` that touches `Assets/`, `Packages/` or `ProjectSettings/`, and can also be run by hand from the Actions tab.

Netlify cannot be pointed at this repository directly. Its build images carry Node, Python and Ruby, not a licensed Unity Editor, so the build has to happen in Actions and Netlify is handed the finished folder.

Five repository secrets are needed. `NETLIFY_AUTH_TOKEN` and `NETLIFY_SITE_ID` are the same two the script uses. The other three are Unity's: `UNITY_LICENSE` (the whole contents of a `Unity_lic.ulf`), `UNITY_EMAIL` and `UNITY_PASSWORD` for the account it was issued to.

The `.ulf` has to come from Unity Hub on a machine you have signed in on — **Preferences → Licenses**. Unity has stopped supporting manual activation of Personal licences, so the old route of asking Unity for an `.alf` from CI and exchanging it for a `.ulf` no longer exists. Note that a Hub showing a valid licence has not necessarily written a `.ulf` to disk; if there is none, re-activate from that screen to make it produce one.

Expect the first run or two to need adjusting — Unity in CI usually does. If the licence cannot be made to work at all, `Tools/deploy-web.sh` does the same job from a machine that is already activated, which is every machine you can build on anyway.

All six arenas ship, plus the dashboard, which has to be index 0 or the build opens straight into a fight. The scene list is passed explicitly rather than read from Build Settings, which still carries Unity's empty `SampleScene`. Each arena is staged as a renamed copy so the touch layer can be baked in without leaving it in the generated scene — which is why a player's stars are filed under the arena's *level set* rather than under the scene name.

**`-fpskitFallback` decides whether the build depends on its host.** Unity ships the payload already Brotli-compressed and needs the server to answer with `Content-Encoding: br`. Leave the fallback on (the default) and a JavaScript decompressor is bundled, so the build runs on any static host — GitHub Pages included — at the cost of a slower start. Turn it off for a host that sends the header itself, which is what the `_headers` file written beside the build configures for Netlify and Cloudflare Pages. Get this wrong and the loading bar never finishes, with nothing in the console to say why.

`VerifyLevels`, `VerifyFlow`, `VerifyCombat` and `VerifyReplay` enter real play mode and fail on any console error.

`VerifyLevels` plays one level to both of its endings. First it drops every enemy through the floor and checks that the leash removes them, that replacements are queued, that the level is still scored when the clock runs out, and that a failed level awards nothing and unlocks nothing. Then it reloads and clears the level outright, and checks that three stars are awarded, stored, and that the next level opens.

`VerifyStore` plays the economy end to end: it funds a profile, opens the store, clicks
the real buttons to buy a gun and two upgrades, checks the second upgrade costs more than
the first, and then loads a level and checks the player is actually holding the gun they
bought with the fire rate they paid for. Then it throws a bomb at an enemy and checks it
died. A purchase is four joins across four files, and breaking any one of them still
*looks* like it worked — the coins go, the card says OWNED, and the player walks in
holding the gun they started with.

`VerifyFlow` plays the loop a player sees: the dashboard offers every arena, clicking one opens its ladder with the right levels locked, level one loads, and quitting lands back on the dashboard with the attempt recorded. It fires a real raycast at every button, because a button whose graphic cannot be raycast is inert and completely silent about it.

`VerifyReplay` plays a fresh session, an in-game restart and a second session after play mode has been stopped and started — the shape that catches state leaking between runs, which this project is prone to because domain reload is disabled.

`VerifyBuild` is the one worth running in CI. A build that throws no exception proves very little — the builder wires dozens of references by hand, and a null one shows up as a black screen or a level that never starts, not as an error. It checks the things that fail silently: the player rig and its camera, the weapon's data asset, the enemy roster and its boss entry, the arena's level set, the bomb thrower and its aim indicator, every store item's prefabs, every HUD binding, the results screen, and whether the NavMesh actually baked.

Every entry point sets its own exit code, so a scene that failed to build fails the run instead of reporting green. The script refuses to start while the Unity editor holds the project lock.

---

## Notes

- Gameplay reads the **legacy `UnityEngine.Input` API**. The one exception is raw mouse look, which uses the Input System's `Mouse.current.delta` when available and falls back to `Input.GetAxisRaw` otherwise. Both paths are kept working on purpose.
- Look sensitivity uses the Valorant/CS convention: degrees turned = counts × sensitivity × 0.022, read raw with no smoothing or acceleration.
- Art comes from the *RPG/FPS Game Assets for PC — Industrial Set* pack under `Assets/RPG_FPS_game_assets_industrial/`. It is third-party material under the Unity Asset Store EULA, not this repository's licence — see [`THIRD_PARTY_NOTICES.md`](THIRD_PARTY_NOTICES.md). The gameplay does not depend on it: arenas are built from primitives by `LevelTheme`, and a fork that deletes the folder still builds and plays.
- Audio in `Assets/Audio/` is synthesised from scratch by `Tools/generate-placeholder-audio.py` with nothing but the Python standard library.

---

## Contributing

Issues and pull requests are welcome. [`CONTRIBUTING.md`](CONTRIBUTING.md) covers the setup, the checks to run before opening a PR, and the five conventions that exist because breaking them produces bugs that compile cleanly — the big ones being that generated content is output rather than source, and that every static needs a reset hook because domain reload is disabled.

[`CLAUDE.md`](CLAUDE.md) is the long version, with the reasoning behind each decision.

---

## Licence

[MIT](LICENSE) — for the code, the editor tooling, the CI, the WebGL template and the generated content.

**Not** for the bundled art pack, which keeps its own terms. [`THIRD_PARTY_NOTICES.md`](THIRD_PARTY_NOTICES.md) says exactly what is covered by which.
